version: '3.8'

services:
  next:
    build: .
    command: sh -c 'yarn install && yarn dev'
    ports:
      - '3020:3020'

  checks:
    build: .
    command: sh -c 'pre-commit run -vvv --all-files'
    profiles:
      - test
